pipeline {
  agent any
  stages {
    stage('Install dependencies') {
      steps {
        sh 'npm ci'
      }
    }
  stage('Quality check') {
    steps {
      sh 'yarn run test:nowatch'
      }
    }
  stage('Build') {
    steps {
      sh 'yarn build'
      }
    }
  }
}